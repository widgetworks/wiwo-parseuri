{
  "name": "wiwo-parseuri",
  "version": "2.0.0-SNAPSHOT",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:widgetworks/wiwo-parseuri.git"
  },
  "main": "parseuri.js",
  "typings": "parseuri.d.ts",
  "desc": "To release, just build and tag the version",
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "xxxrelease": "np --no-yarn --yolo"
  },
  "dependencies": {
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "jest": "^25.1.0",
    "np": "^6.2.0",
    "ts-jest": "^25.2.1"
  }
}
